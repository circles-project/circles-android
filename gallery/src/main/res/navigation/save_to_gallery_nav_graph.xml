<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/save_to_gallery_nav_graph"
    app:startDestination="@id/saveToGalleyDialogFragment">
    <dialog
        android:id="@+id/saveToGalleyDialogFragment"
        android:name="org.futo.circles.gallery.feature.save.SavePostToGalleyDialogFragment"
        tools:layout="@layout/dialog_fragment_save_post_to_gallery">

        <argument
            android:name="roomId"
            app:argType="string"
            app:nullable="false" />

        <argument
            android:name="eventId"
            app:argType="string"
            app:nullable="false" />

    </dialog>
</navigation>